<?php
/**
 * Uninstall VN Story Card plugin.
 *
 * This plugin does not store any data in the database,
 * so no cleanup is needed.
 *
 * @package vn-story-card
 */

// Exit if not called by WordPress.
if ( ! defined( 'WP_UNINSTALL_PLUGIN' ) ) {
	exit;
}
